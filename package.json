{
  "name": "web-kirich-react",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "vercel-build": "npm run build -w @project/shared && npm run build -w ./apps/client",
    "build:server": "tsc -b apps/server/tsconfig.json",
    "start:server": "npm run start --workspace=apps/server"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
